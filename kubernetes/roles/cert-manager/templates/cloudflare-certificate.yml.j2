apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: cloudflare-cert-prod
  namespace: cert-manager
spec:
  secretName: cloudflare-cert-prod
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "*.{{ domain }}"
  dnsNames:
  - "*.{{ domain }}"
  acme:
    config:
    - dns01:
        provider: cf-dns
      domains:
        - "*.{{ domain }}"