terraform {
  backend "s3" {
    bucket                = "{{ bucket }}"
    key                   = "terraform/terraform.tfstate"
    region                = "us-east-2"
    {% if env == 'kops'%}
    workspace_key_prefix  = "{{ workspace }}"
    {% endif %}
  }
}
